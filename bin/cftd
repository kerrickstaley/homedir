#!/bin/bash
# "Claude Fix TODOs"
set -euo pipefail
if [ $# -eq 0 ]; then
    claude --dangerously-skip-permissions "Fix the \"todo claude\" items in this codebase"
else
    claude --dangerously-skip-permissions "Fix the \"todo claude\" items in $1"
fi
