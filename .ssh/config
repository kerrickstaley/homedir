Include config.d/*

Host *
  # AddKeysToAgent means that the SSH agent will load your key
  # Then, if you set ForwardAgent yes for a given host, SSH
  # sessions to that host will be able to use that SSH key
  # (e.g. they will be able to SSH to other hosts you have access
  # to, or access GitHub private repos).
  AddKeysToAgent yes
